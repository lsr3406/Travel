# travel

> A Vue.js project

<!-- 
多页应用: 通过页面跳转返回 html, 首屏时间快(仅一次 http 请求), SEO 效果好, 但页面切换慢
单页应用: 通过 JS 渲染得到 html, 页面切换快, 但首屏时间慢(多一次 js 的 http 请求), SEO 效果不好

reset.css 解决基础样式修饰问题
border.css 解决多倍屏里面1像素边框会被显示多像素的问题需要引入
fastclick库 解决移动端点击300ms延迟问题
安装 npm install fastclick --save,
引入 import fastclick from 'fastclick'
使用 fastclick.attach(document.body);
 -->
